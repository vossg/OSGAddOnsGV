#!python
#
if option_pass:
   print "Processing option pass"
else:
    gv_option = opts.GetOption("enable_gv_beta")

    if gv_option != None and gv_option.value == True:

        library = "OSGContribRRT"
        osg_dep_libs = ['OSGBase', 'OSGSystem', 'OSGGroup', 'OSGDrawable',
                        'OSGWindow']

        # Tests' dependencies
        osg_test_libs = [ 'OSGState', 'OSGFileIO', 'OSGText',
                          'OSGUtil', 'OSGWindowGLUT' ]   
        test_cpppath = []
        test_libpath = []

        if compiler == "ppu-g++":
           libs.append('spe2')
           libs.append('rrt_spu')
           other_test_libs = ['spe2']
           osg_test_libs.append('rrt_spu')

    else:
    
        stop_traversal = True
